<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= title %></title>

  <!-- Tailwind CSS -->
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet"/>

  <style>
    html, body { height: 100%; margin: 0; background-color: #000; }
    .chessboard {
      display: grid;
      grid-template-columns: repeat(8, minmax(0, 1fr));
      grid-template-rows: repeat(8, minmax(0, 1fr));
      width: 420px;
      height: 420px;
    }
    .square { display:flex; align-items:center; justify-content:center; }
    .square.light { background-color: #f0d9b5; }
    .square.dark { background-color: #b58863; }
    .piece {
      font-family: "Noto Chess","Arial Unicode MS","Segoe UI Symbol",sans-serif;
      font-size: 36px; user-select: none; cursor: grab; text-align: center;
    }
    .piece.white { color: #fff; filter: drop-shadow(0 0 2px rgba(0,0,0,1)); }
    .piece.black { color: #000; }
    .videoBox video { background:#111; }
    .btn { @apply px-3 py-2 rounded-lg bg-gray-700 text-white text-sm; }
    .btn:disabled { opacity: 0.5; }
  </style>
</head>
<body class="min-h-screen text-gray-100">
  <div class="max-w-5xl mx-auto p-4 grid grid-cols-1 md:grid-cols-2 gap-6">
    <!-- Board + Status -->
    <div class="space-y-4">
      <h1 class="text-2xl font-bold">Knightly♞</h1>
      <div class="chessboard border-4 border-gray-800 rounded-xl overflow-hidden"></div>

      <div class="flex items-center gap-2">
        <span class="text-sm bg-gray-800 px-2 py-1 rounded">Role:</span>
        <span id="roleLabel" class="text-sm font-semibold">Spectator</span>
        <span class="ml-4 text-sm bg-gray-800 px-2 py-1 rounded">Turn:</span>
        <span id="turnLabel" class="text-sm font-semibold">-</span>
      </div>
    </div>

    <!-- Video + Controls -->
    <div class="space-y-4">
      

      <div class="flex flex-col items-center gap-4 bg-black p-4 rounded-2xl shadow-lg">
        <h2 class="text-lg font-semibold text-white">Players’ Video</h2>
      
        <!-- Local Video -->
        <div class="relative w-2/3">
          <span class="absolute top-2 left-2 bg-black/60 text-white text-xs px-2 py-1 rounded-md">
            You
          </span>
          <video id="localVideo" autoplay playsinline muted 
            class="w-full aspect-video rounded-xl shadow-md object-cover">
          </video>
        </div>
      
        <!-- Remote Video -->
        <div class="relative w-2/3">
          <span class="absolute top-2 left-2 bg-black/60 text-white text-xs px-2 py-1 rounded-md">
            Opponent
          </span>
          <video id="remoteVideo" autoplay playsinline 
            class="w-full aspect-video rounded-xl shadow-md object-cover">
          </video>
        </div>
      </div>
      
      <div class="flex pl-20 gap-2">
        <button id="btnToggleCam" class="btn rounded bg-gray-800">Toggle Cam</button>
        <button id="btnToggleMic" class="btn bg-gray-800">Toggle Mic</button>
        <button id="btnLeaveCall" class="btn rounded bg-red-600">Leave Call</button>
      </div>

      <div id="callStatus" class="text-sm  pl-20 text-gray-300">Waiting for opponent…</div>
    </div>
  </div>

  <!-- scripts -->
  <script src="https://cdn.socket.io/4.8.1/socket.io.min.js" crossorigin="anonymous"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/chess.js/0.10.3/chess.min.js" crossorigin="anonymous"></script>
  <script src="/js/chessgame.js"></script>
</body>
</html>
